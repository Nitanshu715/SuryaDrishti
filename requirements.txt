# Core geo/raster processing
# NOTE: rasterio on Windows/Python3.13 requires pre-built wheels.
# Install separately FIRST using: pip install rasterio --find-links https://girder.github.io/large_image_wheels
# Or install via conda: conda install -c conda-forge rasterio
# The rest of the project works without rasterio (fallback uses Pillow).
rasterio>=1.3.10

# ML & data science  (all Python 3.13 compatible)
scikit-learn>=1.5.0
numpy>=2.0.0
pandas>=2.2.2
scipy>=1.13.0
joblib>=1.4.0

# Image processing
opencv-python>=4.9.0.80
Pillow>=10.3.0
matplotlib>=3.9.0
seaborn>=0.13.2

# Backend API
flask>=3.0.3
flask-cors>=4.0.1

# Utilities
python-dotenv>=1.0.1
tqdm>=4.66.2
