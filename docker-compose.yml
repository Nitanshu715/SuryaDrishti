version: "3.9"

services:
  backend:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - FLASK_ENV=development
      - PORT=5000
    restart: unless-stopped

  # Uncomment to run frontend dev server separately
  # frontend:
  #   image: node:18-alpine
  #   working_dir: /app
  #   volumes:
  #     - ./frontend:/app
  #   ports:
  #     - "3000:3000"
  #   command: sh -c "npm install && npm start"
  #   environment:
  #     - REACT_APP_API_URL=http://localhost:5000
